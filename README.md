# TGProxy CLI

Легковесная и быстрая утилита для установки и управления собственным MTProto Proxy сервером для Telegram. Базируется на современном и надежном движке `mtg`. Поддерживает Ubuntu и Debian.

## Преимущества
* **Быстрая установка** — скрипт скачает, настроит и запустит всё сам за несколько секунд.
* **Поддержка Docker** — возможность развернуть прокси в изолированном контейнере Docker (установит Docker сам, если его нет).
* **Авто-генерация секретов** — безопасный ключ (secret) с Fake-TLS создаётся автоматически.
* **Удобное управление** — просмотр ссылки, рестарт и проверка статуса через простые команды.
* **Без лишнего кода** — оставлен только функционал для MTProto, ничего лишнего.

---

## Установка утилиты

Установите `tgproxy` CLI на свой сервер всего одной командой:

```bash
curl -fsSL https://raw.githubusercontent.com/dntflp/cli/main/install.sh | sudo bash
```

*Скрипт загрузит необходимые файлы, создаст директорию `/opt/tgproxy` и подготовит базовый конфигурационный файл.*

---

## Запуск и настройка прокси

После установки утилиты вы можете выбрать, как именно запустить сам прокси-сервер:

### Способ 1: Стандартная установка (Systemd)
Запуск прокси как обычного системного сервиса `tgproxy.service`.
```bash
sudo tgproxy install
```

### Способ 2: Установка через Docker
Скрипт сам установит Docker (если его нет), сгенерирует секрет и поднимет контейнер с прокси.
```bash
sudo tgproxy install-docker
```

---

## Команды TGProxy

* `tgproxy install` — Установить и настроить MTProto Proxy (как системный сервис).
* `tgproxy install-docker` — Установить и запустить MTProto Proxy в Docker-контейнере.
* `tgproxy link` — Показать вашу личную `tg://` ссылку для подключения к прокси.
* `tgproxy status` — Проверить статус работы сервиса (включая Docker) и просмотреть конфигурацию.
* `tgproxy restart` — Перезапустить системный сервис MTProto (или пересоздать Docker-контейнер для применения нового порта).
* `tgproxy help` — Показать список доступных команд.

---

## Как изменить порт (443) или домен?

Вы можете изменить настройки прокси (включая порт и домен для Fake-TLS), отредактировав конфигурационный файл: `/etc/tgproxy/config.env`.

```bash
sudo nano /etc/tgproxy/config.env
```

Внутри вы найдете строку:
`TGPROXY_PORT=443`

Измените `443` на любой нужный вам порт (например, `8443`).

**После сохранения файла обязательно перезапустите прокси:**
```bash
sudo tgproxy restart
```

Команда `restart` автоматически перечитает настройки. Если вы используете Docker — она **удалит старый контейнер и создаст новый** с указанным вами портом.
